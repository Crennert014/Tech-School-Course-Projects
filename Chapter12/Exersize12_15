package chapter12;
import java.io.*;
import java.util.Scanner;
import java.util.Random;
public class Exersize12_15 {

    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        String fileString = "Exercise12_15.txt";
        int[] randomNum = new int[100];
        Random random = new Random();
        // Random number generator
            for (int i = 0; i < randomNum.length;i++);
                randomNum[i] = random.nextInt(1000);
                }
            saveRandomNum(fileString, randomNum);
                // Save random numbers in file
    public static void saveRandomNum(String fileString, int[] randomNum){
        try (PrintWriter pw = new PrintWriter(new FileWriter(filename, true))) {
            StringBuilder sb = new StringBuilder();
        
            for (int count : eggCounts) {
                sb.append(",").append(count);
            }
            pw.println(sb.toString());
        } catch (IOException e) {
            System.err.println("Error saving entry: " + e.getMessage());
        }
    }
}
